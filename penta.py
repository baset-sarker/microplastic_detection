import numpy as np

penta = np.array([[0,0]], np.int32)
#penta = np.delete(penta, (0), axis=0)

#print(penta)

#penta = np.empty([1,2],dtype=np.int32)
# print(len(penta))
# print(penta)
# cntr=[25,225]
# penta = np.append(penta, [cntr],axis=0)
# #penta = np.delete(penta, (0), axis=0)
# print("====")
# print(len(penta))